/**
 * Site identity. The collection was renamed by the owner after launch; the
 * display name is centralized here. Accession numbers keep their GCC prefix:
 * they are catalog data and shareable URLs, not branding.
 */

export const SITE_NAME = 'The Binford Collection';
